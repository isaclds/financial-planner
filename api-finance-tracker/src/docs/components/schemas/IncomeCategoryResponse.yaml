type: object
required: [success, status, title, data]
properties:
  success:
    type: boolean
    enum: [true]
  status:
    type: integer
    example: 200
  title:
    type: string
    example: OK
  data:
    $ref: './IncomeCategory.yaml'
